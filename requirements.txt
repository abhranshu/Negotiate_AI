# NegotiateAI — Python Requirements
# Install: pip install -r requirements.txt

# ── Core ML / AI ─────────────────────────────
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.27.0
peft>=0.9.0                # LoRA fine-tuning for LLMs
trl>=0.8.0                 # SFT Trainer for LLM fine-tuning

# ── ASR ──────────────────────────────────────
openai-whisper>=20231117
faster-whisper>=1.0.0      # Faster inference alternative

# ── OCR ──────────────────────────────────────
pytesseract>=0.3.10
Pillow>=10.0.0
pdf2image>=1.17.0

# ── NLP / NER ────────────────────────────────
spacy>=3.7.0
# python -m spacy download en_core_web_sm

# ── Outcome Prediction ────────────────────────
xgboost>=2.0.0
lightgbm>=4.3.0
scikit-learn>=1.4.0
shap>=0.44.0
pandas>=2.1.0
numpy>=1.26.0

# ── Backend ───────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9    # file uploads
redis>=5.0.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0            # async postgres driver
alembic>=1.13.0            # migrations

# ── Utilities ─────────────────────────────────
httpx>=0.27.0
python-jose[cryptography]>=3.3.0  # JWT auth
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.0
joblib>=1.3.0
reportlab>=4.1.0           # PDF generation (settlement docs)
faker>=24.0.0              # synthetic data generation

# ── Optional (for local LLM) ──────────────────
# ollama                   # run: curl https://ollama.ai/install.sh | sh
# llama-cpp-python         # local Mistral/LLaMA inference
