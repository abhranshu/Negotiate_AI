# ─────────────────────────────────────────────────────────────
# NegotiateAI — Backend requirements
# Install: pip install -r requirements_api.txt
# ─────────────────────────────────────────────────────────────

# ── FastAPI core ──────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.0.0
pydantic[email]>=2.6.0       # EmailStr support
python-multipart>=0.0.9      # file uploads

# ── Auth ──────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── Database ──────────────────────────────────────
sqlalchemy>=2.0.0
asyncpg>=0.29.0            # uncomment when switching to PostgreSQL
alembic>=1.13.0            # uncomment for DB migrations

# ── Utilities ─────────────────────────────────────
python-dotenv>=1.0.0
httpx>=0.27.0

# ─────────────────────────────────────────────────────────────
# The ML modules (torch, transformers, whisper, etc.) are
# listed in requirements.txt. Install them separately once
# you're ready to enable the full AI pipeline.
# ─────────────────────────────────────────────────────────────
