# ── Deployment requirements (lean — no local Whisper, no GPU packages) ─────────
# These are the ONLY packages needed when deploying to Railway/Render
# Groq API replaces local Whisper → saves ~2GB Docker image size

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0              # production WSGI server
python-multipart>=0.0.9       # file uploads

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0        # PostgreSQL driver (binary = no build tools needed)
alembic>=1.13.0

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.0.0

# ── ML — Prediction (Module 3) ───────────────────────────────────────────────
xgboost>=2.0.0
lightgbm>=4.3.0
scikit-learn>=1.4.0
shap>=0.44.0
pandas>=2.1.0
numpy>=1.26.0
joblib>=1.3.0

# ── NLP — NER (Module 1 entity extraction) ───────────────────────────────────
transformers>=4.40.0
torch>=2.1.0                  # CPU-only on Railway (no CUDA)
spacy>=3.7.0

# ── Voice — Groq Whisper API (replaces local Whisper) ────────────────────────
groq>=0.9.0                   # pip install groq

# ── Document Intelligence (Module 2 — partial) ────────────────────────────────
Pillow>=10.0.0
pytesseract>=0.3.10           # needs tesseract-ocr system package

# ── Settlement PDF (Module 5) ─────────────────────────────────────────────────
reportlab>=4.1.0

# ── Utilities ─────────────────────────────────────────────────────────────────
httpx>=0.27.0
redis>=5.0.0
faker>=24.0.0
